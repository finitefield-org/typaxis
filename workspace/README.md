# Rust reference workspace

This no-external-dependency workspace makes type boundaries and dependency direction compile-checkable. It is not a completed Typaxis implementation. Run `cargo check --workspace --all-targets` and `cargo test --workspace` with Rust 1.75 or later.
