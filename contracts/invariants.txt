I-001 Source ranges are half-open UTF-8 byte ranges tied to SourceId.
I-002 Text ranges are half-open UTF-8 byte ranges tied to TextBufferId.
I-003 Text-map ranges are local to their owning TextBuffer and carry no TextBufferId.
I-004 Text-map segments are non-empty, ordered, non-overlapping, UTF-8 aligned, and cover each buffer exactly.
I-005 Text/source lengths never silently saturate; overflow and configured limits are errors.
I-006 Serialized source/resource declaration paths are PortablePath values resolved beneath an allowed root; ConfigResourceRoot and CLI HostPath are separate types.
I-007 Layout lengths use signed integer units `pdf_point_1_65536`.
I-008 Affine a/b/c/d are signed 16.16 unitless values; e/f are Length.
I-009 Semantic AST contains no glyph, CID, PDF name, or object identifier.
I-010 Break legality is one `BreakKind`; every paragraph item has parsed TextSpan or GeneratedProvenance carrying a GeneratedTextSpan, and every Discretionary branch has explicit drawing content.
I-011 Bidi embedding level survives itemization through Display List generation; every broken line applies UAX #9 L1 reset, final reshape, justification, then L2 visual reorder.
I-012 LayoutPassCoordinator can request re-fragmentation for every frame/reservation/reference state; a materialized state owns the exact pages plus overlay that painted them, while the next LayoutPassInput combines its fingerprint with only a sealed exact-page/anchor/site-bound working-overlay transition carrying the same nonserialized pagination-session capability; begin-pass rejects another session before consuming work, and Paginator never shapes text.
I-013 Page first/parity flags are derived rather than independently stored.
I-014 Fragment continuation either exhausts at the exact terminal or strictly advances to a nonterminal exact FlowTree member within the same epoch; nonblank terminal is the explicit root `End(owner=0,path=[],local=1)`, blank terminal is its sole `DocumentStart`, `More` cannot target either terminal form, owner may change, and opaque fingerprint change is not progress.
I-015 Pagination state 0 is an unselectable seed derived only from the validated package and limits; callers cannot provide its GeneratedTextStore, and pass i produces materialized state i+1.
I-016 Stable state means a pass input fingerprint equals its output fingerprint.
I-017 A cycle repeats a materialized state 1 through pass_count and records its first repeated materialized state index; the domain-separated unmaterialized seed state 0 is neither a cycle target nor a fallback candidate.
I-018 Cycle/max-pass fallback minimizes `(hard_violation_count,total_cost,page_count,state_index)` lexicographically under `lowest_cost_then_earliest`; counts/indices are nonnegative, costs are signed JSON-safe checked integers, and final fingerprint equals that selected state.
I-019 Display List contains logical resource IDs only and every glyph run has explicit paint.
I-020 Every internal link resolves to one unique named destination; each selected named destination is derived exactly once from a package-registered, selected-page/frame-bound anchor placement, and every URI link contains a syntax-validated SafeUri.
I-021 Painted/clipped paths contain a valid state sequence and at least one drawable segment.
I-022 Non-empty dash arrays are not all zero; dash phase is nonnegative.
I-023 Glyph text ownership is cluster-level, not duplicated per glyph; wire Display clusters reference only dense-remapped DisplayTextSpan values.
I-024 CID 0 is reserved for .notdef; each font instance uses CIDs 1 through `max_cids_per_font`, which is at most 65535; Unicode values are valid scalar values.
I-025 CID, subset GID, original GID, Unicode sequence, and ActualText are distinct mappings.
I-026 PDF resource names and object numbers are assigned only by the PDF backend from canonically ordered, backend-identity-free FrozenPdfResourcePlans; font keys are `(font,admitted source SHA-256,FontInstanceId)`, image keys are `(image,admitted source SHA-256,ImageResourceId)`, raw hashes sort lexicographically, IDs sort unsigned numerically, each font's declared Type0/CIDFont/descriptor/program/ToUnicode/CIDToGIDMap traversal is fully preflighted before dense allocation, and the receipt-bound rewritten embedded PostScript name exactly equals every BaseFont/FontName.
I-027 PdfValue cannot contain a stream.
I-028 Every stream is indirect; payload/filter policy is frozen and Length, Filter, and DecodeParms materialization is serializer-owned.
I-029 Duplicate object insertion leaves the builder unchanged, returns an error, and stops the build.
I-030 A frozen PDF graph has no unresolved references; Catalog points to a root Pages node without Parent, and the full Page tree is valid; untrusted direct values and Pages hierarchies use iterative inclusive depth-64 validation with the root PdfValue/stream dictionary/root Pages at depth 1, and recursive values use iterative destruction.
I-031 Link annotations cannot carry raw PDF actions, remain within their page, and convert Rect/destination coordinates outside the content CTM using page height; Catalog Names/Dests and each page Annots collection contain the selected Display destinations/annotations exactly once.
I-032 Canonical JSON integers stay within signed 53-bit exact range unless narrowed further.
I-033 Classic xref offsets are at most 9,999,999,999; output is rejected before an offset of 10,000,000,000 is required.
I-034 Deterministic manifest inputs are unique and sorted by PortablePath UTF-8 bytes, fonts by unique numeric font_face_id, and images by unique numeric image_id; canonical JSON and effective-config hashes use RFC 8785 JCS bytes.
I-035 Release archives contain stored regular-file entries with fixed metadata and a versioned top-level directory independent of checkout name.
I-036 Public product identity is `typaxis`; the CLI binary, build-manifest engine, crate namespace, configuration name, and schema namespace are validated consistently.
I-037 Only note/warning AdvisoryDiagnostics may accompany a phase success value; any error or fatal makes the outcome a failure with no success value or artifact, fatal aborts immediately, and error collection may continue only to a safe phase boundary.
I-038 Resource bytes, hashes, and shaping/image metadata are admitted before shaping; only the crate-owned canonical itemizer may construct ShapeRequest, deriving adjacent context plus bidi/script and accepting Profile 1.0's sole canonical language/features values `None`/empty; requests accept only PackageShapeTextReceipt values binding exact parsed/selected-generated bytes, site/style owner, and document/reference epoch plus a ShapeFontSelectionReceipt binding one validated package/computed style, exact ledger, canonical instance table, computed face, actual font bytes/hash, and exact LayoutEpoch; shaping cache identity also includes ShaperIdentity backend/version and resolved Unicode/Japanese data-table versions; the Display List is PDF-independent, while PDF-specific late finalization cannot assign a backend handle, PDF name, or object ID.
I-039 Style/page-master source_order equals its array's dense 0-based index; winners use documented total precedence and an unresolved exact tie or inheritance cycle is fatal.
I-040 Tables have at least one column and one head-or-body row; cells use source-order leftmost-free placement, spans are positive, in-grid, non-overlapping, cover every row exactly, and do not cross the head/body boundary; repeated headers are leading rows and must fit their frame.
I-041 Line reshape, page lookback, footnote reflow, column balancing, float queue, and float carry use inclusive ResourceLimits: initial footnote fragmentation and initial float-page placement are not reflow/carry, each page-boundary float crossing is one carry, and an unstable final allowed line-reshape pass fails after that pass without starting max+1.
I-042 Affine transforms use column vectors, `x'=a*x+c*y+e`, `y'=b*x+d*y+f`, and `CTM := CTM * M` concatenation.
I-043 Profile 1.0 is always deterministic; effective config precedence is defaults, file, environment, then CLI.
I-044 Identity text-map segments have equal text/source byte lengths and byte-for-byte equal content.
I-045 The build manifest `stream_compression` matches effective-config `pdf_stream_compression`; its `layout` records status/pass count/selected state/final fingerprint/fallback policy, while fallback scores remain trace-only.
I-046 ResourceLimits are positive and satisfy max_source_bytes <= max_input_bytes, max_text_buffer_bytes <= max_text_bytes, max_shaping_context_bytes <= max_text_buffer_bytes, max_font_bytes <= max_resource_bytes, max_image_bytes <= max_resource_bytes, max_fonts <= 26^6, max_cids_per_font <= 65535, and max_output_bytes <= 9,999,999,999; max_fonts/max_images bound unique admitted counts and the font bound makes six-uppercase-letter subset tags total and collision-free.
I-047 Display page_index is dense and 0-based; CLI physical page N is 1-based, N >= 1, and maps by checked N - 1.
I-048 Styleable blocks and selector class components are unique and UTF-8-byte-sorted; selectors are exactly `block_type(.class)*`, reject noncanonical class order, duplicate classes, and unsupported syntax, and match by block type plus class-set inclusion with specificity `(0,class_count,1)`.
I-049 Style IDs are unique and extends references form a known DAG; declaration_order is derived only from the declarations array index, and each matched rule expands root-to-child so property winners maximize `(important,matched specificity,matched source_order,inheritance depth,origin declaration_order)` lexicographically.
I-050 GeneratedBufferKey is the epoch-unique `(owner NodeId, closed ordered generation kind, owner-local ordinal)`; keys are sorted before dense GeneratedTextBufferId allocation, GeneratedProvenance identity uses key plus local range rather than allocation ID, and resolved UTF-8 bytes are pagination-fingerprint input under an algorithm-bearing ReferenceFingerprint JCS domain.
I-051 ConfigResourceRoot is ProjectRoot (wire `.`) or Relative(PortablePath), never a dot PortablePath; admitted roots are resolved config variants plus explicit HostAdmissionContext roots, host paths stay unserialized, one PortablePath resolving under multiple roots is ambiguous rather than first-match, config roots/schemes are unique UTF-8-byte-sorted sets, and admitted logical URIs/bytes/hashes are recorded.
I-052 Page master selection consumes a pre-selection PageSelectionContext with derived first/parity and a distinct requested PageName, then produces a PageContext with one selected MasterId; computed `page: auto` means no requested name, a valid PageName string requests that name, and an effective-name change starts a new page.
I-053 Every ResourceLimits maximum is inclusive: count/size equal to max is allowed, and the operation that would produce max+1 is rejected before reading, allocating, enqueuing, evaluating, or writing it.
I-054 Each materialized layout state owns the immutable GeneratedTextStore that generated its pages, whose canonical GeneratedBufferKey-derived GeneratedTextBufferId namespace is distinct from parsed TextBufferId; a next overlay is never substituted into that state, and the selected paint overlay remains alive through Display construction.
I-055 Display construction places referenced parsed buffers first by TextBufferId, then generated buffers by allocation-independent GeneratedBufferKey; DisplayTextMap performs the checked remap, and DisplayDocument owns the resulting canonical 0-based dense DisplayTextBufferId/DisplayTextSpan text-buffer table.
I-056 Canonical lists have at least one item, require an explicit positive start for ordered lists (resolving an omitted source default to 1), and require null start for unordered lists; every item has one list_marker site whose exact bytes are checked-u32 ASCII `start+index` plus `.` for ordered lists or U+2022 for unordered lists, with spacing represented only as layout Glue.
I-057 ResourceCollector unions repeated use per logical ID; one ID resolving to different admitted hash/metadata is an error, every planned OriginalGlyphId is strictly below the bound admitted font glyph_count, and duplicate canonical plan keys after usage deduplication are finalization errors.
I-058 Canonical build manifests have only terminal built/failed states. Every build owns a non-cloneable one-shot BuildOutputCommitContext, while a same-session ManifestPublicationContext exists only when a manifest target was requested; manifest-free PDF commit remains available, but a configured manifest target requires sealed complete preflight. Failures after that boundary publish output-null facts when the target commit succeeds, while earlier config/usage or pre-publication I/O failure leaves the manifest unchanged. Built output has at least one page, records file/stdout OutputSink without HostPath, and file commit or complete stdout write precedes atomic manifest commit; a directory-sync failure after target publication returns the visible artifact receipt/complete publication as durability-uncertain and never makes a false rollback claim.
I-059 PaginationFingerprintEncoder converts layout state to unique canonically ordered page/frame/fragment/footnote/float/column/placed-anchor/generated-text arrays before RFC 8785 JCS/SHA-256; each anchor's ID, registered owner, page/frame/column, and frame-local point are fingerprinted, and JCS alone is never used to normalize unordered collections or allocation IDs.
I-060 File output, trace, and manifest HostPath targets are pairwise distinct after canonical parent/leaf and available file-identity checks; all identities are re-resolved both when the output session is created and immediately before each write and final atomic publication, and target aliasing is rejected without issuing a receipt regardless of replace policy.
I-061 Profile 1.0 style declarations use only typed font_family, font_size, line_height, and page properties; repeated declarations use the normal important/declaration-order cascade, text materialization requires a family resolved through fully AdmittedResources plus positive size/line height before shaping, and ResourceCatalog family aliases are unique so no ambient or collection-order font fallback is possible.
I-062 EffectiveConfig data-version facts come only from a resolved registered Unicode/Japanese table set, manifest shaper facts are closed linked-registry selection facts rather than proof of actual invocation, and engine facts come only from build-issued EngineIdentity; raw config, CLI, and manifest callers cannot inject unverified identity strings, while any completed-runtime actual-use claim requires a sealed shaper-session receipt in the artifact chain.
I-063 SourceId, TextBufferId, NodeId, FontFaceId, and ImageResourceId use their documented 0-based dense discovery/preorder/declaration allocation; wire array indexes match the owning ID space and worker completion or map order cannot assign IDs.
I-064 Include limits are proven by an IncludeResolver-issued graph receipt with entry depth 0, canonical edges, maximum depth, and source closure; only a sealed source-driven Parser may issue ValidatedParsedPackage, and no flat SourceCatalog, caller-built ParsedPackage, feature, or fixture type can promote itself to that trusted result.
I-065 Document (including the complete canonical ResourceCatalog declaration), style/page-master, admitted-resource, and reference fingerprints come only from their algorithm-bearing canonical JCS encoders; LayoutEpoch and FlowTree positions are derived from those trusted artifacts and validated flow IR rather than caller-chosen hashes or boundary order.
I-066 Shaping, line breaking, pagination, fragmentation, footnote, column, and float work require complete ValidatedResourceLimits-derived budget tokens whose owner rejects max+1 before work, enqueue, or allocation.
I-067 A trusted Display is bound to the selected PaginationResult state and EffectiveConfig policy; resource finalization, PDF construction, and a built manifest accept only the resulting selected-artifact chain and actual sink commit receipt.
I-068 Resource admission and late encoding use bounded resolver/parser/encoder receipts tied to the admitted bytes, metadata, and budget-owning resolver session; foreign-session pending bytes cannot issue a ledger, and the PDF backend consumes selected Display plus frozen plans and alone assigns canonical resource names and dense object IDs.
I-069 A low-level caller-built PDF object graph remains explicitly untrusted; only the PDF backend may preflight the complete object budget, bind selected page/master geometry and exact resource usage, and issue FrozenPdfGraph, while only the serializer may bind its actual bytes/hash, applied stream-compression mode, and EffectiveConfig fingerprint into VerifiedPdfBytesReceipt; output publication rejects another config/artifact chain.
I-070 Nonempty resource admission requires a HostAdmissionContext-bound opaque AdmittedRootSet; each source receipt is same-root-set and binds logical ID, no-follow opened-handle identity, immutable exact extent, streamed SHA-256, and bytes-derived metadata, while missing platform guarantees fail closed.
I-071 FrozenPdfResourcePlans are issued only when the supplied ledger fingerprint equals the selected Display LayoutEpoch admitted-resource fingerprint; another ledger cannot be substituted even when logical IDs match.
I-072 Every embedded CIDFont plan has valid nondegenerate FontBBox, positive StemV, required CapHeight, exactly one of Symbolic/Nonsymbolic, and one collision-free deterministic six-uppercase-letter subset name re-extracted from the rewritten embedded program and shared exactly by Type0, CIDFont, and FontDescriptor.
I-073 Profile 1.0 admits only bounds-checked, duplicate-mandatory-table-free TrueType-outline sfnt/TTC faces with unitsPerEm 16 through 16384 for CIDFontType2/FontFile2; PNG decoded-byte limits measure the checked canonical expanded pixel buffer after legal IHDR validation, never packed scanline bytes.
I-074 max_ast_nesting_depth is positive and at most 64; Document root and root StyleRule have depth 1, typed child and extends edges increment it, exact max is accepted, and max+1 is rejected by an iterative precheck before recursive validation, indexing, fingerprinting, or cascade while unknown/cyclic style inheritance retains its specific error.
I-075 LayoutEpoch and ShapeFontSelectionReceipt are issued only by typaxis-layout-contract below layout and shaping; a different package/style fingerprint, admitted-ledger fingerprint, instance-table ledger, selected FontFaceId, or reference epoch is rejected before shaping, and no raw FontInstanceId, font hash, face index, or font bytes can replace the receipt.
I-076 The contract-defined footnote-1 target numbers markers by one-based canonical FootnoteId-sorted definition order but assigns definitions by selected first-reference order; a page converges only on consecutive equality of body fingerprint, ordered assignments, every continuation, and exact reservation, initial body evaluation is uncharged, max_footnote_reflows_per_page is inclusive with G6002 refusal before max+1, and dedicated strictly advancing FootnoteFlowId carry never enters the body cursor.
I-077 The contract-defined 1.3 advanced-pagination targets keep horizontal-tb/LTR page progression, selected master and header/body-column/footer frame order, region repetition, final-page balance, and FIFO float queue/carry in separate typed receipts; balance candidate, float queue, and float page-carry maxima are inclusive with G6003/G6004 refusal before max+1, and empty/oversize work terminates without a same-position body continuation.
