# Repository instructions

- This repository does not use GitHub Actions or GitHub workflow files.
- Do not create, restore, run, dispatch, or rely on files under `.github/workflows/`.
- Do not invoke GitHub Actions through the GitHub CLI or API.
- Run required verification locally or on explicitly managed hosts, and record the commands and evidence without GitHub Actions.
